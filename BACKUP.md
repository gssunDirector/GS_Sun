# Инструкция по резервному копированию Firebase

## Автоматическое резервное копирование

Используйте скрипт `backup-firebase.sh`:

```bash
# Сделать скрипт исполняемым
chmod +x backup-firebase.sh

# Запустить бэкап
./backup-firebase.sh
```

## Ручное резервное копирование

### 1. Firestore Database

**Способ 1: Через Google Cloud Console (САМЫЙ ПРОСТОЙ) ⭐**

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Вверху выберите проект `azs-project-55e79` (если не выбран)
3. В меню слева (☰) найдите **Firestore** → **Import/Export**
4. Нажмите кнопку **EXPORT** (вверху страницы)
5. Выберите bucket для экспорта (можно создать новый)
6. Нажмите **EXPORT**

Экспорт займет некоторое время. После завершения файлы будут в Storage bucket.

**Способ 2: Через gcloud CLI**

```bash
# 1. Установите gcloud CLI: https://cloud.google.com/sdk/docs/install

# 2. Авторизуйтесь
gcloud auth login

# 3. Выберите проект
gcloud config set project azs-project-55e79

# 4. Экспорт Firestore
gcloud firestore export gs://azs-project-55e79.appspot.com/backups/firestore-$(date +%Y%m%d-%H%M%S)
```

**Способ 3: Через Node.js скрипт (см. файл backup-firestore.js)**

### 2. Authentication (Пользователи)

```bash
firebase auth:export ./backup/auth-users.json --format=json
```

### 3. Storage (Файлы)

**Через Firebase Console:**
1. Откройте **Storage** в Firebase Console
2. Скачайте нужные файлы вручную

**Через gsutil (если установлен gcloud CLI):**
```bash
gsutil -m cp -r gs://azs-project-55e79.appspot.com ./backup/storage
```

### 4. Конфигурация проекта

Скопируйте файлы конфигурации:
```bash
mkdir -p backup
cp firebase.json backup/
cp .firebaserc backup/
```

## Восстановление из бэкапа

### Восстановление Firestore:

```bash
# Через gcloud
gcloud firestore import gs://azs-project-55e79.appspot.com/backups/firestore-YYYYMMDD

# Или через Firebase Console
# Firestore Database → Import → Выберите файл экспорта
```

### Восстановление пользователей Authentication:

```bash
firebase auth:import ./backup/auth-users.json
```

### Восстановление Storage:

```bash
gsutil -m cp -r ./backup/storage/* gs://azs-project-55e79.appspot.com/
```

## Важные замечания

⚠️ **Внимание:**
- Экспорт Firestore может занять время для больших баз данных
- Storage файлы могут занимать много места
- Убедитесь, что у вас достаточно места на диске
- Регулярно делайте бэкапы перед важными обновлениями

📝 **Рекомендации:**
- Делайте бэкап перед каждым деплоем
- Храните бэкапы в безопасном месте
- Тестируйте восстановление на тестовом проекте

## Альтернативные методы

### Использование Firebase Admin SDK

Можно создать Node.js скрипт для экспорта через Admin SDK:

```javascript
const admin = require('firebase-admin');
// ... код для экспорта
```

### Использование Cloud Functions

Можно создать Cloud Function для автоматического бэкапа по расписанию.

